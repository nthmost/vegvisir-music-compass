# The mixtape itself is personal content, not code — deployed to the server
# separately (scp), never committed. Keep audio/README.txt tracked.
audio/*
!audio/README.txt

.DS_Store

# Runtime-generated log report output and IP lookup cache
logs/*.html
logs/*.json
